# Cust
## Building
To build, see ./INSTALL
## Build options
To see build options (configure options), run `./bootstrap` and then `./configure --help` to see the options
## Copyright information
This project is copyrighted under the GNU GPL version 3 or later. The library is licensed under the GNU LGPL version 3 or later. There is an exception for binaries under ./COPYING.RUNTIME
